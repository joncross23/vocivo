{
  "name": "@vocivo/infrastructure",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./in-memory-content": "./src/repositories/in-memory-content.repository.ts",
    "./local-storage-learner": "./src/repositories/local-storage-learner.repository.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@vocivo/content-pipeline": "workspace:*",
    "@vocivo/contracts": "workspace:*"
  }
}
